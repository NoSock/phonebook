import { PluralJsonPipe } from './plural-json.pipe';

describe('PluralJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new PluralJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
